<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net48</TargetFramework>
    <LangVersion>latest</LangVersion>
    <RootNamespace>VsMcp.Extension</RootNamespace>
    <AssemblyName>VsMcp.Extension</AssemblyName>
    <Version>0.13.3</Version>
    <UseCodebase>true</UseCodebase>
    <DeployExtension>false</DeployExtension>
    <CopyBuildOutputToOutputDirectory>true</CopyBuildOutputToOutputDirectory>
    <CopyOutputSymbolsToOutputDirectory>false</CopyOutputSymbolsToOutputDirectory>
    <NoWarn>VSTHRD010;VSTHRD110</NoWarn>
    <AutoGenerateBindingRedirects>true</AutoGenerateBindingRedirects>
    <EnableDefaultCompileItems>false</EnableDefaultCompileItems>
    <!-- Explicit VSSDK properties that need early evaluation -->
    <GeneratePkgDefFile>true</GeneratePkgDefFile>
    <CreateVsixContainer>true</CreateVsixContainer>
    <IncludeAssemblyInVSIXContainer>true</IncludeAssemblyInVSIXContainer>
    <IncludeDebugSymbolsInVSIXContainer>false</IncludeDebugSymbolsInVSIXContainer>
    <IncludeDebugSymbolsInLocalVSIXDeployment>false</IncludeDebugSymbolsInLocalVSIXDeployment>
    <TargetVsixContainerName>VsMcp.Extension.VS2019.vsix</TargetVsixContainerName>
    <!-- Fix IntermediateOutputPath for VSSDK targets in SDK-style project -->
    <IntermediateOutputPath Condition="'$(IntermediateOutputPath)' == ''">obj\$(Configuration)\$(TargetFramework)\</IntermediateOutputPath>
    <CreatePkgDefAssemblyToProcess Condition="'$(CreatePkgDefAssemblyToProcess)' == ''">bin\$(Configuration)\$(TargetFramework)\$(AssemblyName).dll</CreatePkgDefAssemblyToProcess>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.VisualStudio.SDK" Version="16.10.31321.278" ExcludeAssets="runtime" />
    <PackageReference Include="Microsoft.VSSDK.BuildTools" Version="16.11.71" PrivateAssets="all" />
    <PackageReference Include="Microsoft.Windows.SDK.Contracts" Version="10.0.22621.755" />
    <!--
      VS2019 may not have these "mutable" assemblies in its probing path,
      especially on versions older than 16.10. Bundle them in the VSIX
      so the extension is self-contained.
    -->
    <!--
      Threading and Validation are provided by VS2019 at runtime.
      Do NOT bundle them — bundling causes MissingMethodException
      due to type identity mismatch between bundled and VS-loaded assemblies.
      Transitive polyfill DLLs (AsyncInterfaces, Tasks.Extensions, Unsafe)
      are still bundled as VS2019 may not have them.
    -->
    <PackageReference Include="Microsoft.VisualStudio.Threading" Version="16.10.56" ExcludeAssets="runtime" />
    <PackageReference Include="Microsoft.VisualStudio.Validation" Version="16.10.35" ExcludeAssets="runtime" />
    <!-- Polyfill DLLs needed by Threading at runtime; VS2019 may not have them -->
    <PackageReference Include="Microsoft.Bcl.AsyncInterfaces" Version="6.0.0" />
    <PackageReference Include="System.Threading.Tasks.Extensions" Version="4.5.4" />
    <PackageReference Include="System.Runtime.CompilerServices.Unsafe" Version="6.0.0" />
    <!--
      ThreadHelper lives in Shell.Framework in the v16.10 ref assemblies,
      but older VS2019 runtimes may not have it there. Bundle explicitly.
      ExcludeAssets="runtime" to avoid pulling transitive deps into output.
      The DLL is copied to output by CopyShellFrameworkToOutput target below.
    -->
    <PackageReference Include="Microsoft.VisualStudio.Shell.Framework" Version="16.10.31321.278" ExcludeAssets="runtime" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\VsMcp.Shared\VsMcp.Shared.csproj" />
  </ItemGroup>

  <ItemGroup>
    <Reference Include="UIAutomationClient" />
    <Reference Include="UIAutomationTypes" />
    <Reference Include="WindowsBase" />
    <Reference Include="System.Drawing" />
    <Reference Include="System.IO.Compression" />
    <Reference Include="System.IO.Compression.FileSystem" />
    <Reference Include="System.Net.Http" />
    <Reference Include="System.Xml.Linq" />
  </ItemGroup>

  <!-- Linked source files from VsMcp.Extension -->
  <ItemGroup>
    <Compile Include="..\VsMcp.Extension\VsMcpPackage.cs" Link="VsMcpPackage.cs" />
    <Compile Include="..\VsMcp.Extension\McpServer\McpHttpServer.cs" Link="McpServer\McpHttpServer.cs" />
    <Compile Include="..\VsMcp.Extension\McpServer\McpRequestRouter.cs" Link="McpServer\McpRequestRouter.cs" />
    <Compile Include="..\VsMcp.Extension\McpServer\McpToolRegistry.cs" Link="McpServer\McpToolRegistry.cs" />
    <Compile Include="..\VsMcp.Extension\Services\VsServiceAccessor.cs" Link="Services\VsServiceAccessor.cs" />
    <Compile Include="..\VsMcp.Extension\Services\IBrowserConnection.cs" Link="Services\IBrowserConnection.cs" />
    <Compile Include="..\VsMcp.Extension\Services\CdpConnection.cs" Link="Services\CdpConnection.cs" />
    <Compile Include="..\VsMcp.Extension\Services\FirefoxRdpConnection.cs" Link="Services\FirefoxRdpConnection.cs" />
    <Compile Include="..\VsMcp.Extension\Services\FocusGuard.cs" Link="Services\FocusGuard.cs" />
    <Compile Include="..\VsMcp.Extension\Services\FocusAuditor.cs" Link="Services\FocusAuditor.cs" />
    <Compile Include="..\VsMcp.Extension\Services\VsWindowHelper.cs" Link="Services\VsWindowHelper.cs" />
    <Compile Include="..\VsMcp.Extension\Tools\*.cs" Link="Tools\%(Filename)%(Extension)" />
  </ItemGroup>

  <ItemGroup>
    <None Include="source.extension.vsixmanifest">
      <SubType>Designer</SubType>
    </None>
  </ItemGroup>

  <ItemGroup>
    <Content Include="..\VsMcp.Extension\Resources\icon.png">
      <Link>Resources\icon.png</Link>
      <IncludeInVSIX>true</IncludeInVSIX>
      <CopyToOutputDirectory>Never</CopyToOutputDirectory>
    </Content>
    <Content Include="..\..\LICENSE.txt">
      <Link>LICENSE.txt</Link>
      <IncludeInVSIX>true</IncludeInVSIX>
      <CopyToOutputDirectory>Never</CopyToOutputDirectory>
    </Content>
  </ItemGroup>

  <!-- Import VSSDK targets for VSIX packaging -->
  <Import Project="$(NuGetPackageRoot)microsoft.vssdk.buildtools\16.11.71\tools\vssdk\Microsoft.VsSDK.targets" />

  <!-- Publish StdioProxy and include in VSIX -->
  <PropertyGroup>
    <StdioProxyPublishDir>$(ProjectDir)obj\StdioProxy\</StdioProxyPublishDir>
    <CreateVsixContainerDependsOn>IncludeNuGetAssembliesInVsix;PublishStdioProxy;$(CreateVsixContainerDependsOn)</CreateVsixContainerDependsOn>
  </PropertyGroup>

  <Target Name="PublishStdioProxy">
    <Exec Command="dotnet publish &quot;$(ProjectDir)..\VsMcp.StdioProxy\VsMcp.StdioProxy.csproj&quot; -c $(Configuration) -o &quot;$(StdioProxyPublishDir).&quot; --no-self-contained -v quiet" />
    <ItemGroup>
      <VSIXSourceItem Include="$(StdioProxyPublishDir)*.exe;$(StdioProxyPublishDir)*.dll;$(StdioProxyPublishDir)*.json">
        <VSIXSubPath>StdioProxy</VSIXSubPath>
      </VSIXSourceItem>
    </ItemGroup>
  </Target>

  <!--
    Copy Shell.Framework.dll from NuGet cache to output so it can be bundled in VSIX.
    We use ExcludeAssets="runtime" on the PackageReference to avoid transitive deps,
    so we need to copy it manually.
  -->
  <Target Name="CopyShellFrameworkToOutput" AfterTargets="Build">
    <Copy SourceFiles="$(NuGetPackageRoot)microsoft.visualstudio.shell.framework\16.10.31321.278\lib\net472\Microsoft.VisualStudio.Shell.Framework.dll"
          DestinationFolder="$(OutputPath)"
          SkipUnchangedFiles="true" />
  </Target>

  <!--
    Include polyfill assemblies in VSIX.
    Threading and Validation are NOT bundled — VS2019 provides them at runtime.
    Bundling them causes MissingMethodException due to type identity mismatch.
  -->
  <Target Name="IncludeNuGetAssembliesInVsix">
    <ItemGroup>
      <VSIXSourceItem Include="$(OutputPath)Microsoft.VisualStudio.Shell.Framework.dll" />
      <VSIXSourceItem Include="$(OutputPath)Newtonsoft.Json.dll" />
      <VSIXSourceItem Include="$(OutputPath)Microsoft.Bcl.AsyncInterfaces.dll" />
      <VSIXSourceItem Include="$(OutputPath)System.Threading.Tasks.Extensions.dll" />
      <VSIXSourceItem Include="$(OutputPath)System.Runtime.CompilerServices.Unsafe.dll" />
    </ItemGroup>
  </Target>

  <!-- Hook VSIX packaging into the build process -->
  <Target Name="CreateVsixAfterBuild" AfterTargets="Build"
          DependsOnTargets="DetokenizeVsixManifestFile;GeneratePkgDef;CopyPkgDef;CreateVsixContainer" />

</Project>
